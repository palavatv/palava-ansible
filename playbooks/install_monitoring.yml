- hosts: monitoring
  strategy: debug
  remote_user: root
  become: yes
  roles:
    - name: common
      include_role:
        name: common
        tasks_from: monitoring.yml
    - cloudalchemy.prometheus
    - cloudalchemy.grafana
    - cloudalchemy.alertmanager
